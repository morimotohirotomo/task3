<h2>New book</h2>

<%= form_for(@book) do |f| %>

	<div class="field">
		<label>Title</label><br>
		<%= f.text_field :title %>
	</div>

	<div class="area">
		<label>Opinion</label><br>
		<%= f.text_area :body %>
	</div>

	<div class="action">
		<%= f.submit 'Create Book' %>
	</div>

<% end %>